apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - namespace.yaml
  - otel-collector.yaml
  - prometheus.yaml
  - fluentbit.yaml
  - loki.yaml
  - grafana.yaml
  - kbot.yaml

commonLabels:
  app.kubernetes.io/name: kbot-monitoring
  app.kubernetes.io/part-of: monitoring-stack 