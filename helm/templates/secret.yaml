apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-telegram-token
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kbot.labels" . | nindent 4 }}
type: Opaque
data:
  TELE_TOKEN: {{ .Values.telegram.token | b64enc | quote }} 